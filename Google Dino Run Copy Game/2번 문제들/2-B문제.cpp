#include <iostream>
using namespace std;

// 화면 색상 RGB 값
const int screenColorR = 255;
const int screenColorG = 200;
const int screenColorB = 15;

// 바닥 높이
const int floorHeight = 100; // cm

int main() {
    // 화면 색상 설정
    cout << "Setting screen color to RGB(" << screenColorR << ", " << screenColorG << ", " << screenColorB << ")" << endl;

    // 바닥 설정
    cout << "Creating floor with height " << floorHeight << " cm" << endl;

    // 추가적인 로직이나 기능을 구현할 수 있습니다.

    return 0;
}
